#include "main.h"
#include <stdio.h>

/**
 * main - starting point
 * @argc: number of command line arguments
 * @argv: array
 * Description: print name of program
 * Return: Nothing
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
